---
- name: Provision MiniWallet Servers
  hosts: proxmox_vms
  become: true

  roles:
    - common
    - docker

- name: Provision Database Server
  hosts: database_nodes
  become: true

  roles:
    - sql_server